{
  "version": "2.2.4",
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --outfile=dist/main.js",
    "prepare": "husky install",
    "test": "jest"
  },
  "devDependencies": {
    "esbuild": "^0.15.13",
    "eslint": "^7.16.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1",
    "husky": "^8.0.1",
    "jest": "^26.6.3"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^5.1.1"
  }
}
