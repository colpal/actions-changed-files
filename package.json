{
  "version": "2.2.4",
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --outfile=dist/main.js",
    "test": "jest"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run build && git add dist/main.js"
    }
  },
  "devDependencies": {
    "esbuild": "^0.15.13",
    "eslint": "^7.16.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1",
    "husky": "^4.3.6",
    "jest": "^26.6.3"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.0.4",
    "@actions/github": "^4.0.0"
  }
}
