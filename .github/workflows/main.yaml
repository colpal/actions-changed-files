---
name: Main

'on': push

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.3.1
        with:
          fetch-depth: 0

      - run: echo "Previous '${{ github.event.before }}'"

      - run: echo "Current '${{ github.sha }}'"
